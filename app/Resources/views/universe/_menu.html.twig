{% set route = app.request.attributes.get('_route') %}

<nav class="navbar navbar-default">
    <div class="container-fluid">
        <ul class="nav navbar-nav">
            <li class=""> {# {{ route starts with 'app_universe_' ? 'active' }} #}
                <a href="">Lien 1</a> {# {{ path('app_universe_index') }} #}
            </li>
            <li class=""> {# {{ route starts with 'app_rules_' ? 'active' }} #}
                <a href="">Lien 2</a> {# {{ path('app_rules_index') }} #}
            </li>
            <li class=""> {# {{ route starts with 'app_universe_' ? 'active' }} #}
                <a href="">Lien 3</a> {# {{ path('app_universe_index') }} #}
            </li>
            <li class=""> {# {{ route starts with 'app_rules_' ? 'active' }} #}
                <a href="">Lien 4</a> {# {{ path('app_rules_index') }} #}
            </li>
        </ul>
    </div>
</nav>